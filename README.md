Find 10-based number which has a '6' or an '8' in its digits but NOT '6' and '8' at same time. 
(XOR truth table). 
Given start and end integers, return the number of integers between that satisfy this property.